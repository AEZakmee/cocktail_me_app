name: presentation
description: Presentation module
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  domain:
    path: ../domain
  localizations:
    path: ../libs/localizations
  utils:
    path: ../libs/utils
  side_navigation:
    path: ../libs/side_navigation

  get_it: ^7.6.4
  provider: ^6.1.1

  cached_network_image: ^3.3.1
  flutter_blurhash: ^0.8.2
  flutter_snake_navigationbar: ^0.6.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

